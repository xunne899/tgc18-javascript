function calPeriRect(length,width){

    return (length + width) * 2
}



let c = (length,width) => {

    return(length + width) * 2)
}

// if arrow have one line no need return
let d = (length,width) => (length + width) * 2

// if arrow have one argument no need ()
let calAreaofCircle = r => 22/7 * r ** 2


let numbers = [11,22,123,114,55,551,66]
numbers.sort(function(lhs,rhs)){
    return lhs - rhs;
}


console.log(numbers)

numbers.sort ((lhs,rhs) => lhs - rhs)

console.log(numbers)


let foobar = () =>{
    console.log("hello world")
}